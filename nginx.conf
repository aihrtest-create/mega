server {
    listen 80;
    server_name _;
    client_max_body_size 50m; # Increased to 50m for media uploads

    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression for performance
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # Single Page App routing for React (vite base: '/mega/')
    location /mega/ {
        try_files $uri $uri/ /mega/index.html;
    }

    # Redirect root to /mega/
    location = / {
        return 301 /mega/;
    }

    # Proxy API requests to Node.js backend
    location /api/ {
        proxy_pass http://backend:3002/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Proxy Dashboard requests to Node.js backend
    location /dashboard {
        proxy_pass http://backend:3002/dashboard;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /mega/dashboard {
        proxy_pass http://backend:3002/dashboard;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Redirect /analytics to /analytics/
    location = /analytics {
        return 301 /analytics/;
    }

    # Proxy Analytics Dashboard requests to Python backend
    location /analytics/ {
        proxy_pass http://analytics:5001/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
